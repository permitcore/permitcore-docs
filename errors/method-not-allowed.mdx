---
title: "Method not allowed"
description: "405 — The HTTP method is not supported for this path."
---

The HTTP method is not supported for this path.

```json 405 application/problem+json
{
  "type": "https://docs.permitcore.io/errors/method-not-allowed",
  "title": "Method not allowed",
  "status": 405,
  "detail": "…",
  "instance": "/v1/permits",
  "request_id": "req_1f07fb45782b445e9f6312ba23921234"
}
```

Every PermitCore error is [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457)
`application/problem+json`. **Branch on `type` (a stable URI)**, not on `title`
or `detail` (human-readable, may change).

## When it occurs

You used an HTTP verb the endpoint does not implement (e.g. `DELETE` on a read-only listing route).

## How to fix

Use the documented method for the endpoint (see the [API reference](/api-reference/introduction)). Not retryable unchanged.

**Not retryable** — fix the request before resubmitting; retrying unchanged returns the same error.

See the full [error catalog](/errors) for the response shape and retry matrix.
Every response carries an `x-request-id` header — include it in any support request.
