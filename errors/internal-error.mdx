---
title: "Internal server error"
description: "500 — An unexpected error occurred."
---

An unexpected error occurred.

```json 500 application/problem+json
{
  "type": "https://docs.permitcore.io/errors/internal-error",
  "title": "Internal server error",
  "status": 500,
  "detail": "…",
  "instance": "/v1/permits",
  "request_id": "req_1f07fb45782b445e9f6312ba23921234"
}
```

Every PermitCore error is [RFC 9457](https://www.rfc-editor.org/rfc/rfc9457)
`application/problem+json`. **Branch on `type` (a stable URI)**, not on `title`
or `detail` (human-readable, may change).

## When it occurs

An unhandled condition occurred server-side. The full trace is captured internally against your `request_id`; the response body is intentionally generic.

## How to fix

Retry with exponential backoff. If it persists, email support with the `request_id` from the response for fastest triage.

**Retryable** — retry with exponential backoff (respect `Retry-After` on `429`/`503`).

See the full [error catalog](/errors) for the response shape and retry matrix.
Every response carries an `x-request-id` header — include it in any support request.
